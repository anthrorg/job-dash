{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://job.sylphie.live/import-schema.json",
  "title": "Job Search HQ import format",
  "description": "Paste a JSON object of this shape into the dashboard's Import box. Hand this schema to an LLM along with a job posting (or your social-post stats) and have it return matching JSON. Send either array on its own; omit one if it has no items.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "apps": {
      "type": "array",
      "description": "Job applications.",
      "items": { "$ref": "#/$defs/application" }
    },
    "posts": {
      "type": "array",
      "description": "Social posts and their engagement stats.",
      "items": { "$ref": "#/$defs/post" }
    }
  },
  "$defs": {
    "application": {
      "type": "object",
      "required": ["company"],
      "additionalProperties": false,
      "properties": {
        "company": { "type": "string", "description": "Employer name. Required." },
        "role": { "type": "string", "description": "Job title / role." },
        "link": { "type": "string", "description": "URL of the job posting." },
        "dateApplied": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date applied, YYYY-MM-DD. Defaults to today if omitted."
        },
        "stage": {
          "type": "integer",
          "minimum": 0,
          "maximum": 4,
          "description": "Furthest stage reached: 0 Applied, 1 Recruiter Screen, 2 Interview, 3 Final Round, 4 Offer."
        },
        "outcome": {
          "enum": ["active", "rejected", "ghosted", "withdrawn"],
          "default": "active",
          "description": "Current outcome. Use 'active' while still in play."
        },
        "notes": { "type": "string", "description": "Optional free text." },
        "recruiter": {
          "type": ["object", "null"],
          "default": null,
          "additionalProperties": false,
          "description": "Recruiter contact, or null if none.",
          "properties": {
            "name": { "type": "string" },
            "profile": { "type": "string", "description": "LinkedIn or other profile URL." },
            "email": { "type": "string" },
            "phone": { "type": "string" },
            "lastContacted": {
              "type": "string",
              "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
              "description": "Date last contacted, YYYY-MM-DD."
            }
          }
        }
      }
    },
    "post": {
      "type": "object",
      "required": ["title"],
      "additionalProperties": false,
      "properties": {
        "date": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "Date posted, YYYY-MM-DD. Defaults to today if omitted."
        },
        "platform": {
          "type": "string",
          "default": "LinkedIn",
          "description": "e.g. LinkedIn, X, Mastodon."
        },
        "title": { "type": "string", "description": "Post topic or a URL. Required." },
        "impressions": { "type": "integer", "minimum": 0 },
        "reactions": { "type": "integer", "minimum": 0 },
        "comments": { "type": "integer", "minimum": 0 }
      }
    }
  },
  "examples": [
    {
      "apps": [
        {
          "company": "Acme Corp",
          "role": "Senior Engineer",
          "link": "https://acme.example.com/jobs/123",
          "dateApplied": "2026-06-09",
          "stage": 2,
          "outcome": "active",
          "notes": "Referred by a friend.",
          "recruiter": {
            "name": "Jane Doe",
            "profile": "https://linkedin.com/in/janedoe",
            "email": "jane@acme.example.com",
            "phone": "",
            "lastContacted": "2026-06-09"
          }
        }
      ],
      "posts": [
        {
          "date": "2026-06-08",
          "platform": "LinkedIn",
          "title": "Reflections on system design interviews",
          "impressions": 3400,
          "reactions": 120,
          "comments": 18
        }
      ]
    }
  ]
}
